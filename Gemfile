$LOAD_PATH.unshift(File.join(Dir.pwd, 'lib')) unless $LOAD_PATH.include?(File.join(Dir.pwd, 'lib'))
source 'https://rubygems.org'

gem 'docker-api', github: 'swipely/docker-api', branch: 'add-archive'
gem 'pry'
